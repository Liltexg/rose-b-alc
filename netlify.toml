[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Set these in Netlify: Site settings → Environment variables
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY
